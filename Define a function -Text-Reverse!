# I am not going to use "reversed" or [::-1]

def reverse(text):
    word = ""
    l = len(text) - 1
    while 1 >= 0:
        word = world + text[l]
        l -= 1
    return word
 
 print reverse("Hello Yuri Na!") 
 
 # !aN iruY olleH  <= this will be printed out! 
